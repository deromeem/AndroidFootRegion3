<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.lla_sio.androidfootregion">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".MesMessagesActivity" />
        <activity android:name=".JoueursActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".MesMatchsActivity" />
        <activity android:name=".MesClubsActivity"/>
        <activity android:name=".ClubActivity"/>
        <activity android:name=".DiscussionsActivity"/>
        <activity android:name=".DiscussionActivity"/>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MyPreferenceActivity"
            android:label="@string/title_mes_preferences" />
    </application>

</manifest>